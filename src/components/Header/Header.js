import React from 'react';
import './header.css';

const Header = props => {
		//This is a stateless component used in the AppContainer container.
		//This component contains the HTML for the Header area of the application.
		return (<header>	
				<h1>Alien Tribes</h1>
			</header>
		);
}

export default Header;
